# Historical record moved

The CI optimization baseline is archived at [`reports/ci/ci-optimization-baseline.md`](../reports/ci/ci-optimization-baseline.md). Current CI behavior is documented in `ci-cd.md`.

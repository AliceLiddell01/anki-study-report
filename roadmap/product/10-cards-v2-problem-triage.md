# Compatibility pointer — former Stage 10

This path is retained so historical links do not break.

Cards v2 / Problem Triage is now **`C1`**, the next stage of the authoritative [Core product track](../core/README.md).

The former scope remains valid only where it agrees with `C1` and current production contracts. Future decisions must be recorded in the Core track rather than re-creating a linear Stage 10–13 queue.
